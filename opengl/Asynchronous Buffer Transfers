# Asynchronous Buffer Transfers

When Calling OpenGL functions,the drivers translate calls into <font color=yellow> commands </font> and add them into an <font color=red>internal queue on the CPU side</font>.
